# ===== DeepSeek 大模型配置 =====
# 使用方式：复制本文件为 .env.local 并填入密钥
#   未配置密钥时，应用自动运行在 Mock 模式（内置示例数据），保证开箱即用。
#   配置后无需改任何代码，所有 Agent 自动切换到真实大模型输出。
DEEPSEEK_API_KEY=

# 可选：覆盖默认模型。deepseek-chat（通用对话）/ deepseek-reasoner（深度推理）
DEEPSEEK_MODEL=deepseek-chat
